# /mainline

ODIN が ZEUS 本線をそのまま回す。

1. inbound: 本番 `/ai-ops/` の mailto 件名「オペ」を実測。投下はしない。
2. 出品3本: 公開ログインが出品中なら触らない。課金で止める。TT NEED_LOGIN はスキップ。
3. 立花 HT-C: 再送しない。受信箱が無ければ監視不能。
4. hello 件名「オペ」: 受信箱が無ければ監視不能。1件の証拠が来たら即金下書き。

出力はレーンごとの PASS / FAIL / 監視不能 + 残証 URL かパスだけ。
