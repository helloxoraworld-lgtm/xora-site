# /bus

四柱バス。ADAM / YHWH / ZEUS / ODIN。

ZEUS Desktop実体と同じ入口:

```powershell
cd $env:USERPROFILE\Desktop\XORA-BizDev
.\scripts\odin-bus.ps1 recv
```

Cloud / Linux:

```bash
python3 scripts/odin-bus.py recv
```

Grok直通ではない。ZEUSは `営業/判断待ち/ODIN指示_*.md` か Cloud Agent follow-up。PSは走らせない。
本文は残証パスと URL だけ。公開ログインは撃たない。
