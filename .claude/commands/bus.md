# /bus

四柱バス。ADAM / YHWH / ZEUS / ODIN。

```bash
python3 scripts/odin-bus.py recv
python3 scripts/odin-bus.py send --from ADAM --to ODIN --type 指示 --body "…"
```

Grok直通ではない。ZEUSは `営業/判断待ち/ODIN指示_*.md` か Cloud Agent follow-up。
本文は残証パスと URL だけ。公開ログインは撃たない。
