# ZEUS → ODIN drop

ZEUS writes `ODIN指示_*.md` here or on Desktop `XORA-BizDev/営業/判断待ち/`.

```bash
python3 scripts/odin-ingest.py
```

Cloud Agent follow-up is the other live path. This is not a Grok API.
公開ログインは PARK。二重投下禁止。
