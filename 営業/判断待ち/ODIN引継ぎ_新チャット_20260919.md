# ODIN引継ぎ（新チャット用）2026-09-19

会話の中身はCursorの吹き出しではない。**ファイル**を開け。

## 3人のしゃべり（見る場所）

`営業/判断待ち/四柱会議.md`  
通電テスト欄（ADAM / YHWH / ODIN）。

ZEUSへの報告: `inbox/bus/messages/20260919T071832Z__ODIN__ZEUS__残証.md`

会長PC本体: `$env:USERPROFILE\Desktop\XORA-BizDev` → `.\scripts\odin-bus.ps1 recv`  
このCloudの `/home/ubuntu/Desktop/XORA-BizDev` はコピー。本体ではない。

## 新Agentの開き方

- リポ: `helloxoraworld-lgtm/xora-site`
- ブランチ: `cursor/ecc-xora-os-239e`
- PR: https://github.com/helloxoraworld-lgtm/xora-site/pull/2
- 先に読め: `CLAUDE.md` と **このファイル**

## 役割（同じ組織。増やさない）

ZEUS=監視のみ。ODIN=本線1–4＋PR。ADAM=Claude Code。YHWH=Codex。  
3人部屋にZEUSは入らない。公開ログイン PARK。顧客 CLOSED。二重投下禁止。

## 本線（触るな／やれ）

1. inbound CTA **PASS** https://helloxoraworld-lgtm.github.io/xora-site/ai-ops/ 件名「オペ」
2. 出品3本: 公開ログイン側。撃つな。課金関所。
3. 立花: 再送するな。このVMは監視不能。
4. hello受信箱: このVMは監視不能。1件の証拠が来たら即金下書き。
5. site#1 MERGED https://github.com/helloxoraworld-lgtm/xora-site/pull/1

KPI `経理/KPI実測_*.md` と台帳がこのVMに無い = **FAIL**。新ファイルを作るな。Desktop正本を指せ。

## コマンド

```bash
python3 scripts/odin-bus.py recv
python3 scripts/odin-meet.py start --topic "…" --source ZEUS指示
python3 scripts/odin-meet.py report --body "パスとURL"
```

Desktop残証を捏造するな: `odin-bus設置残証_ZEUS_20260919.md` / `四柱運用_ZEUS窓口_20260919.md`
